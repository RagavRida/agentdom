{
  "version": "1.0",
  "host": "netlify.com",
  "name": "Netlify",
  "description": "AgentDOM polyfill manifest for Netlify — sites, deploys, and DNS.",
  "generated_at": "2026-05-14T00:00:00Z",
  "generated_by": "agentdom/handcrafted",
  "source": "polyfill",
  "auth": {
    "method": "oauth2",
    "authorize_url": "https://app.netlify.com/authorize",
    "auth_url":      "https://app.netlify.com/authorize",
    "token_url":     "https://api.netlify.com/oauth/token",
    "scopes": []
  },
  "capabilities": [
    {
      "intent": "sites.list",
      "description": "List sites accessible to the current user.",
      "transport": "api",
      "method": "GET",
      "endpoint": "https://api.netlify.com/api/v1/sites",
      "args": {
        "filter": { "type": "string", "required": false, "description": "all | owner | guest" }
      },
      "side_effects": ["read"],
      "cost": 1
    },
    {
      "intent": "sites.get",
      "description": "Get a site by id.",
      "transport": "api",
      "method": "GET",
      "endpoint": "https://api.netlify.com/api/v1/sites/{site_id}",
      "args": {
        "site_id": { "type": "string", "required": true, "description": "Site id" }
      },
      "side_effects": ["read"],
      "cost": 1
    },
    {
      "intent": "deploys.list",
      "description": "List deploys for a site.",
      "transport": "api",
      "method": "GET",
      "endpoint": "https://api.netlify.com/api/v1/sites/{site_id}/deploys",
      "args": {
        "site_id": { "type": "string", "required": true,  "description": "Site id" },
        "state":   { "type": "string", "required": false, "description": "ready | building | new | error | retrying" }
      },
      "side_effects": ["read"],
      "cost": 1
    },
    {
      "intent": "deploys.trigger",
      "description": "Trigger a new build/deploy from the linked git source.",
      "transport": "api",
      "method": "POST",
      "endpoint": "https://api.netlify.com/api/v1/sites/{site_id}/builds",
      "args": {
        "site_id": { "type": "string", "required": true,  "description": "Site id" },
        "clear_cache": { "type": "boolean", "required": false, "description": "Clear build cache" }
      },
      "side_effects": ["external"],
      "cost": 1
    },
    {
      "intent": "deploys.rollback",
      "description": "Restore a previous deploy as the current published version.",
      "transport": "api",
      "method": "POST",
      "endpoint": "https://api.netlify.com/api/v1/sites/{site_id}/deploys/{deploy_id}/restore",
      "args": {
        "site_id":   { "type": "string", "required": true, "description": "Site id" },
        "deploy_id": { "type": "string", "required": true, "description": "Deploy id" }
      },
      "side_effects": ["external"],
      "cost": 1
    },
    {
      "intent": "env_vars.list",
      "description": "List environment variables for a site.",
      "transport": "api",
      "method": "GET",
      "endpoint": "https://api.netlify.com/api/v1/accounts/{account_id}/env",
      "args": {
        "account_id": { "type": "string", "required": true,  "description": "Account slug or id" },
        "site_id":    { "type": "string", "required": false, "description": "Scope to a specific site" }
      },
      "side_effects": ["read"],
      "cost": 1
    }
  ],
  "meta": {
    "total_operations": 6,
    "api_type": "rest",
    "base_url": "https://api.netlify.com",
    "docs": "https://docs.netlify.com/api/get-started/"
  }
}
